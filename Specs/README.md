# Specs

Specs are the source of truth for Night Shift work.

- `draft-*` files are ignored by Night Shift.
- Any other Markdown spec is considered active after preflight confirms it is
  unambiguous and testable.
- Keep specs bounded enough for one reviewable commit.
- Prefer one to three ready specs per night.

Current specs:

- `01-cli.md`
- `draft-02-llm-enrichment.md`
